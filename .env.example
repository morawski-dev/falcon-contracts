# Copy this file to `.env` (same directory) and fill in your real key.
# `docker compose` reads `.env` automatically and injects the value into the
# backend container as OPENROUTER_API_KEY. The `.env` file is gitignored --
# never commit a real key.
OPENROUTER_API_KEY=
