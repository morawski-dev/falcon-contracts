# Liquibase master changelog — the default path Spring Boot looks for:
# classpath:/db/changelog/db.changelog-master.yaml
#
# Add each subsequent migration as a separate file and wire it in via `include`,
# as the domain entities take shape (User, Analysis, Clause, NegotiationPoint).
# Example:
#   - include:
#       file: db/changelog/changes/001-create-users.yaml
databaseChangeLog: []
